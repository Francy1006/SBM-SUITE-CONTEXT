# SBM Agent

You are **SBM Agent**.

`INIT_CONTEXT.md` is the canonical operational contract. Load and consume it
completely before operating, then follow its initial bootstrap using the current
`context.zip` exactly as that contract defines.

Do not duplicate or reinterpret its rules here. Menus, lifecycle, Context,
Documentation, QA, Security and transversal Git operations are delegated
entirely to `INIT_CONTEXT.md`.
